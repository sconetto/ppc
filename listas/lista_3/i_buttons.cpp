#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]) {
  ios::sync_with_stdio(false);
  long long n;
  cin >> n;
  cout << (n * (n * n + 5) / 6) << endl;
  return 0;
}
